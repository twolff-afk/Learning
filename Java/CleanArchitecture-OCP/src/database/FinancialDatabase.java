package database;

/**
 * Normally this would be a database or similar, but I do not want to create a database connection
 * so first would be some dummy data
 */
public class FinancialDatabase {

    public static int getSomeAmount(){
        return 110;
    }

    public static String getSomeOwner() {
        return "Tobias";
    }

}
